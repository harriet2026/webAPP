# Country flag sprite

`flags-4x3.png` is generated from the 4x3 SVG assets in `lipis/flag-icons`.
The 26 × 26 sprite is ordered by ISO alpha-2 code: first letter selects the row,
second letter selects the column. Source SVGs are MIT licensed; the license text
is retained in `LICENSE.flag-icons`.
