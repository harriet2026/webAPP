---
name: "html-spec-generator"
description: "基于用户当前描述、可获得的历史对话记录以及正在运行的 webapp，生成静态 HTML 可视化规格说明到 webapp/doc/html-spec/。webapp 同时是原型与实际项目，是当前 UI 和可观察行为的事实源；规格保留交互预览，但交互预览只能由浏览器实拍的状态截图序列及对应的触发路径、元素、字段、结果、权限和数据/API 信息组成，不复制业务组件 HTML，不提供可操作控件或脚本。生成或重生成时必须通过浏览器逐状态验证，清除旧交互残留，并复核历史差异与待确认事项。Invoke when user asks for 生成HTML规格、html_spec、HTML规格说明、可视化规格文档、从 webapp 生成 HTML spec、组件规格 HTML。"
---

# HTML 静态可视化规格说明生成技能

## 职责

以 `webapp/` 中正在运行的页面为当前 UI 与可观察行为的事实源，生成可直接在浏览器打开的静态 HTML 规格说明，输出到：

```text
webapp/doc/html-spec/<模块名>/
```

`webapp` 同时承担原型和实际项目角色，不再存在“把独立 demo 组件复制进规格文档”的需要。规格文档的职责是记录事实和要求，而不是再次实现一套界面。

每个规格必须使用：

- 浏览器实拍截图；
- 由操作前后状态截图组成的交互预览；
- 与截图对应的页面区域、组件、元素和字段说明；
- 状态触发路径与交互后的可观察结果；
- 权限、产品形态、校验、异常、数据和 API 信息；
- 目标需求与 webapp 当前状态的差异；
- 仍需确认的问题及复核记录。

以下内容明确禁止：

- 从 webapp 提取 `outerHTML` 嵌入规格；
- 在规格中复制 React/Tailwind 业务组件；
- 为按钮、输入框、弹窗、表格等绑定模拟业务交互；
- 用内联 mock、状态机或脚本在规格中重演 webapp；
- 为复制出的组件做 DOM 树逐节点一致性校验。

普通文档能力（目录锚点、超链接、预渲染流程图或内嵌 SVG）不属于业务组件交互，可以使用。

> 本技能只生成规格，不修改 `webapp/src/`、后端实现、测试用例或原始需求文档，也不自动提交代码。

## 触发条件

用户提出以下请求时使用本技能：

- 基于 webapp 生成 HTML 规格说明；
- 为某个页面、路由或组件生成/更新 `html_spec`；
- 生成 HTML 规格、可视化规格文档、组件规格 HTML；
- 在 webapp 变化后同步已有 HTML 规格。

## 事实源与优先级

### 需求裁决顺序

1. 用户当前描述；
2. 当前上下文中可获得的、时间更晚且明确的用户决定；
3. webapp 当前运行态与源码；
4. 其他原始 spec，仅作非权威差异对照。

历史对话不可获得时，明确写“历史对话记录：当前上下文未提供”，不得虚构结论。

### 当前实现事实源

- 浏览器中实际运行的 webapp 是 UI、文案、布局、可见性、禁用态和交互结果的第一事实源。
- `webapp/src/` 是补充事实源，用于确认类型、状态管理、隐藏分支、i18n key、API 调用和浏览器无法直接观察的信息。
- 源码推断与浏览器结果冲突时，当前实现按浏览器结果记录，同时标注冲突。
- 用户目标与 webapp 当前状态冲突时，同时记录“目标要求”和“当前实现”，不得用当前实现覆盖目标。

## 核心产物规则

### 1. 截图式交互预览 + 对应信息

交互预览必须保留，但只能是只读的截图预览。每条关键交互用“触发前截图 → 操作说明 → 触发后截图/状态截图序列”表达；简单交互可以只展示结果态截图并引用前置状态。

每个重要页面状态都必须形成一组完整记录：

| 内容 | 要求 |
|---|---|
| 状态名称 | 明确说明默认态、打开态、选中态、错误态、空态等 |
| 触发路径 | 从哪个页面状态，经什么点击、输入、悬停或权限切换进入 |
| 浏览器截图 | 使用该状态的真实渲染截图，不使用手绘占位图 |
| 可见元素 | 文案、图标、位置、默认值、占位符、状态、禁用条件 |
| 交互结果 | 操作后页面发生什么变化、反馈是什么、如何恢复 |
| 数据/API | 相关字段、请求、响应映射；无法确认时标注待确认 |
| 权限差异 | 对应产品形态和角色下的可见、只读、隐藏情况 |
| 验证依据 | 浏览器操作、DOM/可访问性信息、源码路径或用户决定 |

截图不能替代文字信息，文字信息也不能替代截图。审查者应能从一组“截图 + 表格 + 说明”独立理解该状态。

交互预览的 HTML 容器使用 `.interaction-preview`，内部只放标题、触发路径、顺序编号、`figure/img/figcaption`、说明和表格。所有截图直接铺开显示，不使用轮播、折叠、热点点击或弹层查看。

### 2. 交互预览只使用截图

HTML 规格是只读文档，不是第二套 webapp。生成文件时：

- 不创建 `.component-preview`；
- 可以创建 `.interaction-preview`，但其中只能包含静态截图和对应信息；
- 不嵌入 webapp 组件 HTML/CSS；
- 不复制 Tailwind utility class 来还原业务组件；
- 不加入模拟业务行为的 `onclick`、表单状态或 mock API；
- 不要求在生成后的规格中点击、输入、排序、分页或开关弹窗；
- 不引用 Tailwind CDN。
- 不包含 `<button>`、`<input>`、`<select>`、`<textarea>`、`<form>`、`<details>`、`<summary>` 或 `<script>` 等可交互标签；
- 不保留 `onclick`、`onchange`、`onsubmit`、`addEventListener` 等事件绑定；
- 更新旧规格时直接删除旧组件 DOM、事件脚本、mock 数据和专用交互 CSS，不得仅用 `display:none` 隐藏。

交互必须在真实 webapp 中验证，然后用截图式交互预览和对应信息记录。截图中出现的按钮、输入框等只是像素，不是规格文档中的真实控件。

### 3. 交互预览状态覆盖

按实际功能选择并覆盖重要状态，至少检查：

- 默认态、加载态、空态、正常数据态；
- Hover、Focus、选中、禁用、只读；
- 校验失败、提交失败、权限不足、后端异常；
- 弹窗/抽屉打开态、表单联动态、确认态；
- 排序、分页、筛选、批量操作前后；
- 关键响应式断点；
- 主题、locale、产品形态和角色差异。

没有该状态或无法安全触发时，注明“不适用”或“无法验证”，不要伪造截图。

### 4. 产品形态 × 角色差异矩阵

页面级规格必须包含 `<section id="form-role-matrix">`，记录当前功能在各形态与角色下的可见性和可编辑性。

产品形态：

| 简称 | 全称 |
|---|---|
| AI-单 | AI版-单租户 |
| 传统-单 | 传统版-单租户 |
| 云-多 | 云网关-多租户 |
| AI-多 | AI版-多租户 |
| 传统-多 | 传统版-多租户 |

角色：

- 平台管理员；
- 租户管理员。

统一图例：

| 符号 | 含义 |
|---|---|
| ✅ | 显示且可编辑 |
| 🔒 | 显示但只读 |
| ❌ | 隐藏 |
| — | 该形态无此角色 |
| ? | 当前无法验证，必须列入待确认事项 |

每个组件和元素表增加“形态/角色差异”列。无差异时使用 `[Base]`；差异使用 `[Overlay]`、`[单租户]`、`[多租户]` 等标记。

### 5. 逐元素详细度

不要只写“有一个搜索框”。根据元素类型记录足以落地和验收的信息：

- 通用元素：类型、文案、图标、位置、尺寸、默认值、占位符、禁用条件、交互结果、i18n key；
- 输入控件：数据类型、必填、长度、格式、边界值、校验时机和错误文案；
- 表格：列序、列名、字段、宽度、对齐、排序、格式、空值、溢出、固定列；
- 弹窗/抽屉：触发条件、尺寸、标题、字段、按钮、关闭方式、遮罩和焦点行为；
- 状态反馈：loading、toast、错误、空态、重试、成功后刷新或跳转；
- 权限：入口可见性、只读/禁用条件、无权访问结果。

### 6. 输出目录

```text
webapp/doc/html-spec/
├── index.html                         # 总索引
├── assets/                            # 可选共享静态资源
└── <模块名>/
    ├── index.html                     # 模块主规格
    ├── version.json                   # 模块版本元数据
    ├── screenshots/
    │   ├── page-default.png
    │   ├── dialog-open.png
    │   └── validation-error.png
    └── state-<N>-<描述>.html          # 可选；状态很多且主文件过长时使用
```

规则：

- 根目录只放总索引和共享资源，不平铺模块主文件或截图。
- 优先在模块 `index.html` 中连续展示截图式交互预览。
- 只有状态很多、主文件明显难以审查时才拆为 `state-*.html`。
- 更新已有模块时，可保留既有 `layer-*.html` 文件名以避免链接失效，但其内容必须改为截图式交互预览，不得保留可执行交互。
- 主文件和状态子文件都用 `./screenshots/<文件名>` 引用截图。

### 7. 必备章节

页面级规格至少包含：

1. 元信息与事实源；
2. 产品形态 × 角色矩阵；
3. 组件/区域目录；
4. 功能概述；
5. 页面布局与默认态整页截图；
6. 逐组件/区域的截图式交互预览；
7. 弹窗、抽屉和关键状态截图；
8. 数据模型与 API 映射；
9. 业务逻辑与权限；
10. 交互结果与异常场景；
11. 国际化与响应式；
12. 关联功能模块；
13. 差异标注；
14. 需确认事项；
15. 版本历史。

组件级规格可以省略不适用的页面章节，但仍需包含事实源、状态截图、元素信息、交互结果、差异和待确认事项。

### 8. 差异与问题闭环

重生成时，先读取已有规格中的全部 `D-*` 和 `Q-*` 条目，逐项复核：

- `已解决`
- `部分解决`
- `未解决`
- `无法验证`

每项保留原编号和历史描述，并记录：

- 本次状态；
- 复核日期；
- 验证依据；
- 当前结论；
- 处理说明或解决版本。

不得批量写“均已处理”，不得静默删除已解决条目。其他原始 spec 只能帮助说明差异，不能单独证明问题已解决。

## 版本管理

每个模块使用 `version.json` 作为版本号权威来源。新文件建议使用：

```json
{
  "module": "email-handling-disposal-center",
  "current_version": 2,
  "history": [
    {
      "version": 2,
      "generated_at": "2026-07-27",
      "webapp_commit": "abcdef1",
      "comparison_spec_commit": "not-used",
      "summary": "补充弹窗校验失败态截图和字段说明"
    }
  ]
}
```

规则：

- 重生成时读取 `current_version` 后加 1；
- `generated_at` 使用真实日期；
- `webapp_commit` 记录当前仓库中该页面/组件相关代码的短 hash；
- 未读取对照 spec 时，`comparison_spec_commit` 写 `not-used`；
- 已有历史若使用 `demo_commit` / `spec_commit`，保留旧记录，不伪造迁移；新增记录使用新字段；
- 技能不执行 `git commit` 或 `git push`；
- 历史文件通过主仓库 git 回溯，例如：

```bash
git log --oneline -- webapp/doc/html-spec/<模块>/
git checkout <commit> -- webapp/doc/html-spec/<模块>/
```

## 工作流

### Phase 0：确认范围与现有产物

1. 确认用户指定的是页面路由还是组件。
2. 在 `webapp/src/` 中定位路由、组件、i18n、权限和数据请求代码。
3. 读取模块已有的 `index.html`、状态子文件、`version.json`、`D-*` 和 `Q-*`。
4. 如需使用其他原始 spec，明确标记“仅作差异对照，不作为需求依据”。
5. 读取当前目录适用的工程约束，例如 `AGENTS.md`、`DESIGN.md`。

### Phase 1：启动并验证真实 webapp

1. 确认 `http://localhost:3000` 可访问。
2. 未运行时执行：

```bash
cd webapp && npm run dev
```

3. 访问实际 locale 路由，例如 `http://localhost:3000/zh/<route>`。
4. 完成登录、租户、产品形态和角色切换等前置条件。
5. 逐项执行真实交互，观察 DOM/可访问性信息，并按操作顺序截取触发前、触发后和关键中间态。
6. 所有截图保存到模块的 `screenshots/` 目录，建议命名：

```text
<区域>-<状态>-<形态>-<角色>-<视口>.png
```

7. 对可能产生真实写入、删除、发送等副作用的操作：
   - 优先使用安全测试数据或只读路径；
   - 未得到授权时不要执行破坏性操作；
   - 无法安全验证时记录“无法验证”及原因。

### Phase 2：提取对应信息

对每条截图式交互预览建立状态记录，至少提取：

1. 截图对应的路由、locale、主题、视口、形态、角色；
2. 触发前置条件与完整操作路径；
3. 页面区域、组件和元素清单；
4. 文案、图标、默认值、校验、禁用和可见性；
5. 操作后的页面变化、反馈、恢复路径；
6. 相关源码路径、i18n key、数据字段、API；
7. 用户目标、历史决定与 webapp 当前状态的差异；
8. 无法从浏览器或源码确认的事项。

浏览器负责证明“实际呈现和行为”，源码负责补充“内部映射和条件”。不要因为已有源码就跳过浏览器验证。

### Phase 3：生成静态 HTML

1. 创建或复用模块目录和 `screenshots/`。
2. 以 [`references/html-template.md`](./references/html-template.md) 为静态文档骨架。
3. 为默认态和每条关键交互生成 `.interaction-preview`：按顺序嵌入触发前/后截图、状态元信息、元素表和交互结果。
4. 生成产品形态 × 角色矩阵。
5. 补充数据/API、业务逻辑、权限、国际化、响应式和关联模块。
6. 更新差异与待确认事项的逐项复核结果。
7. 更新 `version.json` 和模块中的版本历史。
8. 更新 `webapp/doc/html-spec/index.html` 的模块卡片、版本和日期。

生成的 HTML 必须：

- 内嵌文档 CSS，或引用 `webapp/doc/html-spec/assets/` 中的本地共享 CSS；
- 不依赖 Tailwind 或其他业务 UI 框架；
- 不包含复制的 webapp 业务组件；
- 不包含任何 JavaScript、可操作表单控件或模拟业务交互；
- 截图使用相对路径；
- 离线打开时仍可完整阅读；
- 图表使用静态表格、预渲染图片或内嵌 SVG，不在规格中加载 Mermaid 脚本。

### Phase 4：验证规格文档

用浏览器打开生成后的模块 `index.html` 及所有状态子文件，验证：

- 文件和图片链接有效；
- 截图式交互预览的顺序、触发路径和结果信息匹配；
- 文档没有复制业务组件或模拟交互控件；
- 表格、目录、长文本和图片在常见视口下可读；
- 形态/角色矩阵完整；
- `D-*`、`Q-*` 没有遗漏；
- 版本元信息与 `version.json` 一致；
- 总索引链接有效。

对模块目录执行残留扫描，命中后必须清理再验收：

```bash
rg -n '<(button|input|select|textarea|form|details|summary|script)\b|component-preview|on(click|change|input|submit|key|mouse)[a-z]*\s*=|addEventListener\s*\(|tailwindcss' webapp/doc/html-spec/<模块>/ -g '*.html'
```

允许的交互仅限普通 `<a>` 文档导航。残留扫描应无输出；不能用注释、隐藏节点或无效事件处理器保留旧交互。

这里验证的是静态文档完整性，不在规格文档内回放 webapp 交互。业务交互只在 Phase 1 的真实 webapp 中验证。

## 关键约束

1. 必须通过浏览器验证真实 webapp，不能只读源码。
2. 不修改 webapp 实现；产物只写入 `webapp/doc/html-spec/`。
3. 必须生成截图式交互预览，但不生成可操作组件，不提取 `outerHTML`，不复制业务组件。
4. 每个重要状态必须有真实截图、触发路径和对应信息。
5. 无法观察或安全验证的内容必须明确标注，禁止臆造。
6. 用户当前描述优先，webapp 负责说明当前实现，其他 spec 只作差异对照。
7. 规格文件保持静态、无脚本、可离线阅读。
8. 更新旧规格时彻底清除交互控件、事件、mock 和专用样式残留。
9. 不自动提交或推送。
