# GT-14265 发信人黑白名单观察模式

## 1. 需求背景

发信人黑白名单模块的规则一旦启用即会立即对命中邮件执行拦截、隔离或放行动作。用户在新建规则或调整已有规则的匹配范围时，缺少一种"先观察命中情况、再正式生效"的过渡手段，容易因规则配置不准确而误伤正常邮件或漏拦风险邮件。

本需求在发信人黑白名单模块新增"观察模式"能力：规则开启观察模式后仅记录命中日志，不实际执行规则动作，帮助用户在正式启用前验证规则的影响范围。

本次为纯前端交付，使用 mock 数据模拟观察模式的开启/关闭状态，暂不涉及后端接口改造。

## 2. 需求目标

- 在"新建/编辑"抽屉的"基础设置"区域，增加观察模式的启用/禁用开关。
- 在规则列表增加"观察模式"表头字段，并支持直接在列表中启用/禁用。
- 观察模式状态与规则的启用/禁用状态（`is_active`）互相独立，两者可分别控制。
- 复用项目现有国际化框架和 `Switch` 组件形态，不引入新的交互范式。
- 不修改发信人黑白名单之外的其他功能模块。

## 3. 最终实现方案

### 3.1 数据模型

- `SenderFilterRuleView` 增加只读字段 `observe_mode: boolean`，随行数据一起下发给列表和抽屉。
- `SenderFilterFormData` 增加可选字段 `observe_mode?: boolean`，供抽屉表单读写。
- 由于后端暂无承载该状态的字段，`observe_mode` 的值由 `SenderFilterPage` 维护的本地 mock 态注入到每一行，不进入规则的真实 CRUD 请求体，不随规则的创建、编辑、删除请求持久化到后端。

### 3.2 新建/编辑抽屉——基础设置

- 在"基础设置"区域、启用状态字段附近增加"观察模式"字段：
  - 使用项目现有 `Switch` 组件，`checked` 绑定表单字段 `observe_mode`。
  - 字段标签旁附带帮助图标和 Tooltip，说明"启用后规则仅记录命中情况，不实际执行动作，用于验证规则效果"。
  - Switch 右侧展示当前状态文案（启用/禁用），文案随开关状态实时切换。
  - 新建规则默认 `observe_mode` 为关闭（`false`）；编辑已有规则时，表单从该行的 `observe_mode` 回填。

### 3.3 规则列表——观察模式列

- 在规则列表表头增加"观察模式"列，位于现有状态列附近。
- 单元格使用 `Switch` 组件直接展示和切换该行的观察模式状态，`checked` 绑定 `row.original.observe_mode`。
- 切换操作调用页面级 `onToggleObserve(id, observeMode)` 回调，更新对应规则行的本地 mock 状态，不影响该行其余字段。
- Switch 的 `aria-label` 使用国际化的启用/禁用文案，保证可访问性。

### 3.4 国际化

- 在 `senderFilter` 命名空间新增 `observeMode`（"观察模式"）和 `observeModeHelp`（帮助文案）两个键。
- 中文资源使用 UTF-8 编码，不引入 Unicode replacement character 或异常全角问号字符。
- 同步在英文、俄文、泰文资源文件补齐对应翻译，保持四语言 key 集合一致。

### 3.5 Mock 数据与测试

- Mock 数据层为规则行附加初始 `observe_mode` 值，覆盖开启和关闭两种状态，便于前端联调和视觉验证。
- 补充/更新单元测试，覆盖抽屉表单默认值、编辑回填、列表 Switch 渲染与切换回调三个关键路径。

## 4. 功能范围与实现映射

| 功能点 | 主要实现文件 | 说明 |
| --- | --- | --- |
| 数据类型 | `src/types/sender-filter.ts` | `SenderFilterRuleView.observe_mode`、`SenderFilterFormData.observe_mode`。 |
| 新建/编辑抽屉 | `src/components/security/sender-filter/SenderFilterDrawer.tsx` | 基础设置区域新增观察模式 Switch 及帮助 Tooltip，表单校验与默认值/回填逻辑。 |
| 规则列表 | `src/components/security/sender-filter/SenderFilterTable.tsx` | 新增"观察模式"列，列内 Switch 绑定 `onToggleObserve` 回调。 |
| 页面状态管理 | `src/components/security/SenderFilterPage.tsx` | 维护观察模式的本地 mock 状态，串联抽屉表单与列表切换。 |
| Mock 数据 | `src/lib/api/sender-filter.ts`、`src/lib/mock/storage.ts` | 为规则行提供初始 `observe_mode` mock 值。 |
| 国际化 | `messages/zh.json`、`messages/en.json`、`messages/ru.json`、`messages/th.json` | `senderFilter.observeMode`、`senderFilter.observeModeHelp` 四语言文案。 |
| 单元测试 | `src/components/security/sender-filter/SenderFilterDrawer.test.tsx`、`src/components/security/sender-filter/SenderFilterTable.test.tsx`、`src/lib/api/sender-filter.test.ts` | 覆盖表单默认值/回填、列表渲染与切换、mock 数据结构。 |

## 5. 验收标准

| 编号 | 验收项 | 结果 |
| --- | --- | --- |
| AC-14265-01 | 新建/编辑抽屉的基础设置区域展示观察模式启用/禁用开关。 | 通过 |
| AC-14265-02 | 编辑已有规则时，观察模式开关回填该规则当前的观察模式状态。 | 通过 |
| AC-14265-03 | 新建规则时，观察模式默认关闭。 | 通过 |
| AC-14265-04 | 规则列表新增"观察模式"表头字段，展示每条规则当前的观察模式状态。 | 通过 |
| AC-14265-05 | 规则列表中的观察模式开关支持直接启用/禁用，且不影响该行其他字段。 | 通过 |
| AC-14265-06 | 观察模式文案使用项目国际化框架，覆盖中、英、俄、泰四语言。 | 通过 |
| AC-14265-07 | 未修改发信人黑白名单之外的其他功能模块。 | 通过 |
| AC-14265-08 | 本次改动未引入 Unicode replacement character 或异常全角问号字符。 | 通过 |

## 6. 变更范围

本需求仅覆盖发信人黑白名单模块新建/编辑抽屉的基础设置区域、规则列表表头与交互、相关数据类型、mock 数据以及对应国际化文案。不改变发信人黑白名单以外的其他安全策略模块，不改变现有规则的创建、编辑、删除、筛选、分页、导入导出等业务逻辑，不涉及后端接口改造。

## 7. 验证记录

- 已完成涉及文件的 TypeScript 类型检查，未发现本次修改范围内的类型错误。
- 已运行发信人黑白名单相关单元测试（抽屉表单、列表渲染、mock 数据），全部通过。
- 已校验中、英、俄、泰四份语言资源文件 JSON 格式有效。
- 已在浏览器预览中验证：新建/编辑抽屉基础设置区域展示观察模式开关，规则列表展示"观察模式"列且开关可正常切换。
- 已对本次改动文件执行异常字符检查，未发现 Unicode replacement character 或异常全角问号字符。
